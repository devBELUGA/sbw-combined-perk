// Copy this file to `secret.gradle` and fill values.
// `secret.gradle` is local-only
ext {
    secret_modrinth_project_id = "YOUR_MODRINTH_PROJECT_ID"
    secret_modrinth_token = "YOUR_MODRINTH_TOKEN"
}
